# devtrack-api

RESTful API for DevTrack - syncs project tickets from Notion, maps them to client-facing features, and serves progress data to internal teams and clients via magic link access.

Project list responses include additive per-project `progressSummary` data so overview screens can render progress without per-project ticket fan-out.
